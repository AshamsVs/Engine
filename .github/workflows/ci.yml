name: Documentation Engine CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # IMPORTANT for Git history

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install markdown jinja2 weasyprint gitpython

      # STEP 1 — Generate combined JSON
      - name: Generate combined data
        run: |
          python combine_data.py
          mv combined_data.json combined_data_v3.json

      # STEP 2 — Run exporter
      - name: Run Documentation Exporter
        run: |
          python exporter.py \
            --data combined_data_v3.json \
            --template templates/report_template.md \
            --out output/report

      # STEP 3 — Upload output folder
      - name: Upload report artifacts
        uses: actions/upload-artifact@v4
        with:
          name: documentation-report
          path: output/
