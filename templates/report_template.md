# 📘 Project Documentation Report  
Generated automatically by the AutoDoc Engine (Static + AI Hybrid)

---

## 🏷 Project Information

**Project Path:**  
`{{ data.metadata.project_root | default("unknown") }}`  

**Generated At:**  
`{{ data.metadata.generated_at | default("unknown") }}`  

**Engine Version:**  
`{{ data.metadata.version | default("unknown") }}`  

**Match Mode:**  
`{{ data.metadata.match_mode | default("strict") }}`  

---

# 🟦 1. Abstract  
{{ data.report.ai_summary.abstract | default("_AI summary not available._") }}

---

# 🟩 2. Project Overview  
{{ data.report.ai_summary.overview | default("_AI summary not available._") }}

---

# 🟧 3. System Architecture  
{{ data.report.ai_summary.architecture | default("_AI summary not available._") }}

---

# 🟪 4. Important / Frequently Modified Files  
{{ data.report.ai_summary.top_files | default("_AI summary not available._") }}

---

# 🟨 5. Module Summaries  
{{ data.report.ai_summary.file_summaries | default("_AI summary not available._") }}

---

# 🟥 6. Recommendations  
{{ data.report.ai_summary.recommendations | default("_AI summary not available._") }}

---

# ⚙️ 7. Technical Summary (Static Analysis)

**Files analyzed:** `{{ data.summary.num_files }}`  
**Functions detected:** `{{ data.summary.num_functions }}`  
**Commits processed:** `{{ data.summary.num_commits }}`  

---

# 🧹 8. Code Quality Overview

{% set q = data.quality_issues | default({}) %}
{% if q %}
{% for file, issues in q.items() %}
### {{ file }}

- Missing docstrings: `{{ issues.missing_docstrings | length }}`
- Long functions: `{{ issues.long_functions | length }}`
- Too many parameters: `{{ issues.too_many_parameters | length }}`
- Deep nesting: `{{ issues.deep_nesting | length }}`
- TODO comments: `{{ issues.todo_comments | length }}`

{% endfor %}
{% else %}
_No major quality issues detected by static analysis._
{% endif %}

---

# 🚫 9. Unused Functions  
{% if data.unused_functions %}
{% for fn in data.unused_functions %}
- {{ fn }}
{% endfor %}
{% else %}
_No unused functions found._
{% endif %}

---

# 🔁 10. Duplicate Functions  
{% if data.duplicate_functions %}
{% for file, items in data.duplicate_functions.items() %}
- **{{ file }}** → {{ items|length }} duplicate(s)
{% endfor %}
{% else %}
_No duplicate functions detected._
{% endif %}

---

# 🕒 11. Commit Timeline (Summary)

{% if data.commit_history %}
{% for c in data.commit_history %}
- **{{ c.commit }}** — {{ c.message }}  
  _({{ c.date }})_
{% endfor %}
{% else %}
_No commit history available._
{% endif %}

---

# 📂 12. Important Files (Static Summary)

{% for f in data.files %}
### 📌 File: `{{ f.path | default("unknown") }}`

- **Functions:** `{{ f.functions | length }}`
- **Changes:** `{{ f.file_history | length }}`

{% endfor %}

---

# 🏁 END OF REPORT  
Generated by AutoDoc Engine.
